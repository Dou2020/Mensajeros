---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/navbar.astro';
import InicioList from '../components/inicioList.astro';
import Footer from '../layouts/footer.astro';

const pageMetadata = {
  title: 'Inicio - Iglesia Sinai Retalhuleu',
  description: 'Sitio web oficial de Iglesia Presbiteriana Sinai - Retalhuleu, Guatemala. Conoce nuestros eventos, células y actividades.',
  keywords: 'iglesia sinai, sinai retalhuleu, iglesia guatemala, jovenes sinai, iglesia presbiteriana',
  author: 'Iglesia Sinai Retalhuleu'
};
---

<Layout 
  title={pageMetadata.title}
  description={pageMetadata.description}
  keywords={pageMetadata.keywords}
  author={pageMetadata.author}
>
  <Navbar option="I" />
  
  <!-- Hero Section -->
  <section class="relative overflow-hidden bg-gradient-to-b from-blue-50 to-white dark:from-gray-900 dark:to-gray-800">
    <div class="container mx-auto px-4 py-12 md:py-20">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 dark:text-white mb-6 tracking-tight animate-fade-in">
          Bienvenidos a 
          <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
            Iglesia Sinai
          </span>
        </h1>
        <p class="text-lg md:text-xl text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto">
          Un lugar donde encontrarás comunidad, fe y esperanza. Hay un espacio reservado para ti en cada una de nuestras actividades.
        </p>
      </div>
    </div>
  </section>

  <!-- Próximos Eventos Section -->
  <section class="container mx-auto px-4 py-12 md:py-16">
    <header class="mb-12 text-center md:text-left">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-4">
        Próximos Eventos
      </h2>
      <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl">
        Únete a nuestras próximas actividades. Mantente informado de todo lo que está sucediendo en nuestra comunidad.
      </p>
    </header>
    
    <InicioList />
  </section>
  
  <Footer />
</Layout>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }
</style>

